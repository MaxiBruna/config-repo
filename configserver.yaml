server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MaxiBruna/config-repo.git
          search-paths: '{application}', 'common'
          clone-on-start: true